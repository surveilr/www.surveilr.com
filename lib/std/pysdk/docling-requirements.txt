# Docling Integration Dependencies  
# For surveilr AI Context Middleware with Type-Safe ORM

# Core ORM - Best Python ORM with excellent type safety
sqlalchemy>=2.0.0,<3.0.0

# Main Docling library
docling>=2.0.0

# Core data types and transformations
docling-core>=1.0.0

# Data validation and type safety
pydantic>=2.0.0
typing-extensions>=4.0.0

# Development tools (optional)
mypy>=1.5.0              # Static type checker
sqlalchemy[mypy]         # SQLAlchemy MyPy plugin

# Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# ULID generation for ID consistency with surveilr
python-ulid>=2.2.0

# Alternative async database drivers (optional)
# aiosqlite>=0.19.0      # For async SQLite
# asyncpg>=0.29.0        # For async PostgreSQL